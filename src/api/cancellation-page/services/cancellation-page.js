'use strict';

/**
 * cancellation-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::cancellation-page.cancellation-page');
