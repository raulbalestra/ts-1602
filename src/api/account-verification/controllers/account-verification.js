'use strict';

/**
 * account-verification controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::account-verification.account-verification');
